<h1><strong>New Game</strong></h1>
<div class="d-flex">
  <% @letters.each do |letter| %>
      <div class="d-inline p-1 bg-primary text-white m-2 text-align-center" style="width: 40px; text-align: center; box-shadow: 5px 5px 5px black;" >
        <%= letter %>
      </div>
        <%  end %>
</div>
<h3>What is the longest word you can find?</h3>

<form action="score" method="post" data-turbo="false" >
  <!-- ... -->
  <input type="text" name="score" placeholder="Type your word">
  <input type="submit" value="play">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
</form>
